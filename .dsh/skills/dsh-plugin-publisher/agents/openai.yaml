interface:
  display_name: "DSH Plugin Publisher"
  short_description: "打包安装并验证 DeepSeek Harness 插件"
  default_prompt: "使用 $dsh-plugin-publisher 打包、安装并验证一个 DSH 插件。"

policy:
  allow_implicit_invocation: true
